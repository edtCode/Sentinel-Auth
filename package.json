{
  "name": "sentinel-auth-monorepo",
  "version": "1.1.0",
  "private": true,
  "description": "SentinelAuth backend — Express + PostgreSQL auth service, deploys to Render.",
  "scripts": {
    "dev:server": "npm --prefix backend run dev",
    "test": "npm --prefix backend test",
    "start:backend": "npm --prefix backend start"
  },
  "license": "ISC"
}
